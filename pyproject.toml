[project]
name = "qmt-auto-boot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pywinauto>=0.6.8",
    "PyYAML>=6.0.1",
    "pywin32>=306; sys_platform == 'win32'",
    "ddddocr>=1.4.7",
]
